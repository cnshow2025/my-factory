# my-factory

單一 HTML 檔的測試機台排程模擬器，`index.html` 就是全部，沒有建置步驟。

## 分支

**只用 `main`，不開功能分支。** 所有修改直接 commit 到 `main` 並推上去。

GitHub Pages 是從 `main` 出的（https://cnshow2025.github.io/my-factory/），
推到別的分支網站不會更新。

## 改動前先看

- `docs/wip/README.md` — 載入貨批的欄位定義與資料檔產生方式
- `docs/report/README.md` — 分析報告的圖表與版面產生方式

改了模擬規則或參數，`README.md` 要一起更新。
